# DBSCAN Clustering – README

This project demonstrates **DBSCAN (Density-Based Spatial Clustering of Applications with Noise)** using Python. The notebook includes data loading, preprocessing, model training, visualization, and cluster analysis.

## 📌 Features

* Implements DBSCAN clustering algorithm
* Detects core points, border points, and noise
* Visualizes clusters using matplotlib
* Allows tuning of parameters like `eps` and `min_samples`

## 🚀 Requirements

Install the required Python libraries:

```
pip install numpy pandas matplotlib scikit-learn
```

## ▶️ How to Run

1. Open the `DBSCAN.ipynb` notebook in Jupyter Notebook / JupyterLab / VS Code.
2. Run all cells sequentially.
3. Modify DBSCAN parameters to observe different clustering results.

## 📂 Files

* **DBSCAN.ipynb** – Main notebook with implementation and visualization.

